# Frontend ↔ Backend Contract — Subscriptions API

> Source of truth: this document is built **only** from the code in
> `back/app/`. Every endpoint, request shape, response shape, status code,
> header, and error here can be traced to a file in this repo. Provider
> integration internals (Kite SOAP, Tele2 REST, Moabits Orion) are out of
> scope — the frontend talks to **this** API, not to providers.

- Base URL: configured per environment (e.g. `https://api.example.com`)
- API version prefix: **`/v1`** (every business endpoint listed below)
- Health endpoints: `/health`, `/ready` (no `/v1` prefix)
- Content type: `application/json` for all bodies; errors are
  `application/problem+json` (RFC 7807)
- Auth scheme: `Authorization: Bearer <access_token>` (JWT HS256)
- All responses include:
  - `X-Request-ID` — echoed from request or generated by the server
  - `X-API-Version: v1`

Implementation pointers:
- Router wiring: [app/main.py](app/main.py)
- Request ID + version headers: [app/shared/middleware.py](app/shared/middleware.py)
- Error envelope (problem+json): [app/main.py:60-98](app/main.py#L60-L98) and [app/shared/errors.py](app/shared/errors.py)

---

## Table of contents

1. [Conventions](#conventions)
2. [Authentication & user/profile](#1-auth-identity--profiles)
   - `POST /v1/auth/signup`
   - `POST /v1/auth/login`
   - `POST /v1/auth/refresh`
   - `POST /v1/auth/logout`
   - `GET /v1/me` / `PUT /v1/me`
   - `GET /v1/users` / `POST /v1/users` / `GET /v1/users/{user_id}` / `PUT /v1/users/{user_id}` / `DELETE /v1/users/{user_id}`
3. [Tenancy — companies & settings](#2-tenancy--companies--settings)
   - `GET /v1/companies/me` / `PUT /v1/companies/me`
   - `GET /v1/companies/me/settings` / `PUT /v1/companies/me/settings`
   - `GET /v1/companies` / `POST /v1/companies` (admin)
   - `GET /v1/companies/{company_id}` / `PUT /v1/companies/{company_id}` / `DELETE /v1/companies/{company_id}` (admin)
4. [Tenancy — provider credentials](#3-tenancy--provider-credentials)
   - `GET /v1/companies/me/credentials`
   - `GET /v1/companies/me/credentials/{provider}`
   - `POST /v1/companies/me/credentials/{provider}/test`
   - `PATCH /v1/companies/me/credentials/{provider}`
   - `DELETE /v1/companies/me/credentials/{provider}`
   - `GET /v1/companies/me/provider-mappings/moabits`
   - `GET /v1/companies/provider-mappings/moabits`
   - `GET /v1/companies/provider-mappings/moabits/source-companies`
   - `GET /v1/companies/provider-mappings/moabits/discover`
   - `PUT /v1/companies/{company_id}/provider-mappings/moabits`
   - `DELETE /v1/companies/{company_id}/provider-mappings/moabits`
   - `GET /v1/admin/credentials`
   - `GET /v1/admin/companies/{company_id}/credentials`
   - `GET /v1/admin/companies/{company_id}/credentials/{provider}`
   - `PATCH /v1/admin/companies/{company_id}/credentials/{provider}`
   - `DELETE /v1/admin/companies/{company_id}/credentials/{provider}`
   - `POST /v1/admin/companies/{company_id}/credentials/{provider}/test`
   - `POST /v1/admin/companies/{company_id}/credentials/{provider}/probe`
5. [SIMs](#4-sims)
   - `GET /v1/sims`
   - `POST /v1/sims/search`
   - `GET /v1/sims/{iccid}`
   - `GET /v1/sims/{iccid}/usage`
   - `GET /v1/sims/{iccid}/presence`
   - `PUT /v1/sims/{iccid}/status`
   - `POST /v1/sims/{iccid}/purge`
   - `POST /v1/sims/import`
   - `POST /v1/sims/details` *(ADR-012 — implemented)*
   - `POST /v1/sims/export` *(ADR-012 — pending implementation)*
6. [Async jobs & sync](#5-async-jobs--sync) *(ADR-012 — implemented for routing sync; export pending)*
   - `GET /v1/jobs/{job_id}`
   - `GET /v1/sync/status`
   - `POST /v1/sync/trigger`
7. [Providers — capabilities](#6-providers--capabilities)
   - `GET /v1/providers/{provider}/capabilities`
8. [Health](#7-health)
9. [Domain enums (frontend reference)](#8-domain-enums)
10. [Error envelope & known error codes](#9-error-envelope-rfc-7807)

---

## Conventions

### Headers (every authenticated request)

| Header | Required | Notes |
|---|---|---|
| `Authorization: Bearer <jwt>` | yes | Issued by `/auth/login`, `/auth/signup`, `/auth/refresh`. See [app/identity/dependencies.py:18](app/identity/dependencies.py#L18). |
| `Content-Type: application/json` | for `POST/PUT/PATCH` with body | |
| `X-Request-ID` | optional | If omitted, server generates one and echoes it back. Use one per logical user action so logs/audits/idempotency can be correlated. See [app/shared/middleware.py:32](app/shared/middleware.py#L32). |
| `Idempotency-Key` | required for write SIM lifecycle ops | Required for `PUT /v1/sims/{iccid}/status` and `POST /v1/sims/{iccid}/purge`. Window: 24h. See [app/subscriptions/routers/sims.py:191-196](app/subscriptions/routers/sims.py#L191-L196) and [app/shared/errors.py:198-201](app/shared/errors.py#L198-L201). |

### Headers in every response

| Header | Notes |
|---|---|
| `X-Request-ID` | echoed/generated request id |
| `X-API-Version` | always `v1` |
| `Retry-After` | only on `429 provider.rate_limited` and `503 provider.unavailable` when the upstream provider supplied a hint. Value is in **seconds** (integer string). The same value is also mirrored inside the problem+json body as `extra.retry_after`. See [app/main.py:74-77](app/main.py#L74-L77). |

### CORS

Allowed origins come from `CORS_ORIGINS` env var (default
`http://localhost:3000,http://localhost:5173`). All methods and headers are
allowed; `allow_credentials=true`. See [app/main.py:51-57](app/main.py#L51-L57).

### Identifiers & data types

- `id`, `company_id`, `user_id`: **UUIDv4** strings
- All datetimes: **ISO-8601 UTC**, e.g. `2026-04-01T00:00:00Z`
- Some SIM-list filters (`modified_since`, `modified_till`) require the
  exact pattern `yyyy-MM-ddTHH:mm:ssZ` (no fractional seconds, trailing `Z`
  mandatory). Pattern enforced at [app/subscriptions/routers/sims.py:69](app/subscriptions/routers/sims.py#L69).

### Roles (RBAC)

Defined as `AppRole` in [app/identity/models/profile.py:12-16](app/identity/models/profile.py#L12-L16).

| Role | Typical use |
|---|---|
| `admin` | Company admin. Full write access (settings, credentials, lifecycle writes, user management). |
| `manager` | Read provider credentials/SIMs; manage members. Cannot rotate credentials nor execute lifecycle writes. |
| `member` | Standard tenant user. Read SIMs (no lifecycle writes); edit only own profile. |
| `public` | Bootstrap/non-tenant. Has **no** `company_id`; cannot use any tenant-scoped endpoint (returns 403 — see [app/identity/dependencies.py:80-93](app/identity/dependencies.py#L80-L93)). |

The role required by each endpoint is listed inline below.

---

## 1. Auth, identity & profiles

Implementation: [app/identity/routers/auth.py](app/identity/routers/auth.py),
[app/identity/routers/me.py](app/identity/routers/me.py),
[app/identity/routers/users.py](app/identity/routers/users.py).

### 1.1 `POST /v1/auth/signup`

Two modes — anonymous (creates a brand-new company) or authenticated
(an admin/manager invites a user into the caller's company).

**Auth**: optional Bearer token (no token = anonymous mode).

**Request body** ([SignupRequest](app/identity/routers/auth.py#L30-L35)):
```json
{
  "email": "owner@acme.com",
  "password": "supersecret",
  "full_name": "Jane Doe",         // optional
  "company_name": "Acme",          // REQUIRED in anonymous mode; ignored in authenticated mode
  "role": "member",                 // ignored in anonymous mode (always becomes "public");
                                    // in authenticated mode: defaults to "member"
  "company_id": "uuid"              // authenticated admin only: REQUIRED target company;
                                    // manager requests always use the manager's own company
}
```

Rules ([app/identity/routers/auth.py:94-176](app/identity/routers/auth.py#L94-L176)):
- Anonymous: `company_name` required → creates `Company` + `CompanySettings({})` + `Profile(role=public)`
- Authenticated as `admin`: `company_id` required; `role` may be any of `admin|manager|member|public`
- Authenticated as `manager`: `company_id` is not chosen by the client; target is always the manager's own company; `role` may be only `member` or `public`
- Authenticated as `member` or `public`: 403

**Response 201** — same envelope as `/login` (see § 1.2).

**Errors**:
- `400` `company_name required for signup without authentication`
- `400` `Invalid role: <role>`
- `422` `company_id is required when creating a user as admin`
- `403` `Cannot invite users without a company scope` / `Only admins and managers can create users` / `Managers can only create members or public users`
- `409` `Email already exists` / `Company name already exists`

### 1.2 `POST /v1/auth/login`

**Auth**: none.

**Request**:
```json
{ "email": "owner@acme.com", "password": "supersecret" }
```

**Response 200** ([_build_token_response](app/identity/routers/auth.py#L54-L68)):
```json
{
  "access_token": "<JWT>",
  "token_type": "bearer",
  "expires_in": 3600,                                  // seconds; controlled by JWT_EXPIRE_MINUTES (default 60)
  "refresh_token": "<opaque-random-base64>"            // store securely; rotates on /refresh and /logout
}
```

JWT payload is `{ "sub": "<user_uuid>", "exp": ... }` signed HS256 with the
server `JWT_SECRET`. The frontend should treat it as opaque — do not depend
on additional claims.

**Errors**: `401 Invalid credentials`.

#### JWT bearer validation (every authenticated endpoint)

When a request carries `Authorization: Bearer <jwt>`, the server validates it and may return:
- `401 {"detail": "Token expired"}` — JWT signature is valid but `exp` has passed. The frontend should attempt `POST /v1/auth/refresh` with the stored `refresh_token` and retry the original request once.
- `401 {"detail": "Invalid token"}` — JWT signature/structure invalid or `sub` missing. Force a re-login (clear stored tokens).
- `401 {"detail": "User not found"}` — the JWT is well-formed but the referenced profile no longer exists in the database (e.g. user deleted by an admin while logged in). Force a re-login.

See [app/identity/dependencies.py:18-44](app/identity/dependencies.py#L18-L44). These errors use FastAPI's default `{"detail": ...}` shape — not `application/problem+json`.

### 1.3 `POST /v1/auth/refresh`

Single-use rotation: the supplied refresh token is deleted and a new one
returned in the body.

**Request**:
```json
{ "refresh_token": "<previous-refresh-token>" }
```

**Response 200**: same `TokenResponse` shape as `/login`.

**Errors**: `401 Invalid refresh token` / `401 Refresh token expired`.

### 1.4 `POST /v1/auth/logout`

**Request**:
```json
{ "refresh_token": "<active-refresh-token>" }
```

**Response 204** (no body). The refresh token (if it exists) is removed
server-side. Logout silently succeeds even when the token is not found
(prevents user-enumeration through error timing).

### 1.5 `GET /v1/me`

**Auth**: any authenticated profile.
**Response 200** ([ProfileOut](app/identity/schemas/profile.py#L9-L17)):
```json
{
  "id": "uuid",
  "company_id": "uuid|null",
  "email": "user@acme.com|null",
  "role": "admin|manager|member|public",
  "full_name": "string|null",
  "created_at": "2026-04-01T00:00:00Z"
}
```

### 1.6 `PUT /v1/me` / `PATCH /v1/me`

Both methods are accepted on the same endpoint.

**Request** ([ProfileUpdate](app/identity/schemas/profile.py#L20-L23)):
```json
{
  "full_name": "New name",    // optional
  "email": "new@acme.com",    // optional — must be unique across all users
  "password": "newpassword"   // optional — empty string is ignored
}
```
All fields are optional. Only provided fields are updated.
**Response 200**: `ProfileOut`.
**Errors**: `409 Email already exists` when the new email is already taken.

### 1.7 `GET /v1/users`

**Auth**: `admin` or `manager`.

**Query parameters**:
| Param | Type | Default | Notes |
|---|---|---|---|
| `page` | int | `1` | Page number (1-based). |
| `size` | int | `50` | Items per page. |
| `q` | string | `null` | Filter by `full_name` or `email` (case-insensitive `ILIKE`). |

Note: managers only see `manager` and `member` profiles; `admin` profiles are hidden from manager-scoped queries.

**Response 200**: `Page[ProfileOut]` — paginated list of profiles in the caller's company:
```json
{
  "items": [ /* ProfileOut[] */ ],
  "total": 12,
  "page": 1,
  "size": 50,
  "pages": 1
}
```

### 1.8 `POST /v1/users`

**Auth**: `admin` or `manager`. `public` and `member` → 403.
**Request** ([UserCreate](app/identity/schemas/user.py#L6-L10)):
```json
{
  "email": "user@acme.com",
  "password": "pwd",
  "full_name": "Optional",        // optional
  "role": "member",                // default "member"; admin can pick any, manager only "member"
  "company_id": "uuid"             // admin only: REQUIRED target company;
                                    // manager requests always use the manager's own company
}
```
**Response 201**: `ProfileOut`.
**Errors**: `422 company_id is required when creating a user as admin` / `403 Insufficient permissions` / `403 Managers can only create members` / `409 Email already exists`.

### 1.9 `GET /v1/users/{user_id}`

**Auth**: any authenticated profile (members can only read themselves).
**Response 200**: `ProfileOut`.
**Errors**: `403 Insufficient permissions`, `404 User not found`.

### 1.10 `PUT /v1/users/{user_id}` / `PATCH /v1/users/{user_id}`

Both methods are accepted on the same endpoint.

**Auth/rules**:
- `member`: only on own id, cannot change `role`
- `manager`: only members in own company; cannot promote past `member`; cannot edit admins
- `admin`: full access within own company

**Request** ([UserUpdate](app/identity/schemas/user.py#L13-L17)):
```json
{
  "full_name": "string|null",                        // optional
  "role": "admin|manager|member|public|null",        // optional — see rules above
  "email": "newemail@acme.com|null",                 // optional — must be unique
  "password": "newpassword|null"                     // optional — empty string is ignored
}
```
All fields are optional. Only provided (non-null) fields are updated.
**Response 200**: `ProfileOut`.
**Errors**: `403 Members cannot change roles`, `403 Managers can only edit members`, `403 Managers cannot promote beyond member`, `404 User not found`, `409 Email already exists`.

### 1.11 `DELETE /v1/users/{user_id}`

**Auth**: `admin` only. Cannot delete self.
**Response 204** (no body).
**Errors**: `400 Cannot delete your own account`, `404 User not found`.

---

## 2. Tenancy — companies & settings

Implementation: [app/tenancy/routers/companies.py](app/tenancy/routers/companies.py).
All endpoints are scoped to the **caller's** company (`/me`).

### 2.1 `GET /v1/companies/me`

**Auth**: any authenticated profile in a company.
**Response 200** ([CompanyOut](app/tenancy/schemas/company.py#L8-L13)):
```json
{ "id": "uuid", "name": "Acme", "created_at": "2026-04-01T00:00:00Z" }
```

### 2.2 `PUT /v1/companies/me`

**Auth**: `admin`.
**Request** ([CompanyUpdate](app/tenancy/schemas/company.py#L16-L17)):
```json
{ "name": "Acme Corp" }
```
**Response 200**: `CompanyOut`.

### 2.3 `GET /v1/companies/me/settings`

**Auth**: any authenticated profile in a company.
**Response 200** ([CompanySettingsOut](app/tenancy/schemas/company.py#L20-L25)):
```json
{
  "company_id": "uuid",
  "settings": { /* arbitrary JSON object — frontend defines its keys */ },
  "updated_at": "2026-04-01T00:00:00Z"
}
```

### 2.4 `PUT /v1/companies/me/settings`

**Auth**: `admin`.
**Request** ([CompanySettingsUpdate](app/tenancy/schemas/company.py#L28-L29)):
```json
{ "settings": { /* full replacement object */ } }
```
**Response 200**: `CompanySettingsOut`. The provided `settings` object replaces the previous value (no merge).

### 2.5 `GET /v1/companies`

List all companies in the platform.

**Auth**: `admin` only.

**Query parameters**:
| Param | Type | Default | Notes |
|---|---|---|---|
| `page` | int | `1` | Page number (1-based). |
| `size` | int | `50` | Items per page. |
| `q` | string | `null` | Filter by company name (case-insensitive `ILIKE`). |

**Response 200**: `Page[CompanyOut]` — paginated list of all companies, ordered by `created_at desc` then name:
```json
{
  "items": [ { "id": "uuid", "name": "Acme", "created_at": "2026-04-01T00:00:00Z" } ],
  "total": 5,
  "page": 1,
  "size": 50,
  "pages": 1
}
```

### 2.6 `POST /v1/companies`

Create a new company.

**Auth**: `admin` only.
**Request** ([CompanyCreate](app/tenancy/schemas/company.py#L16-L17)):
```json
{ "name": "Acme" }
```
- Name is trimmed and normalized; empty string → `422`.
- A `CompanySettings({})` row is created alongside the company.

**Response 201**: `CompanyOut`.
**Errors**: `409 Company name already exists`, `422 name cannot be empty`.

### 2.7 `GET /v1/companies/{company_id}`

**Auth**: `admin` only.
**Response 200**: `CompanyOut`.
**Errors**: `404 Company not found`.

### 2.8 `PUT /v1/companies/{company_id}`

Rename a company by ID.

**Auth**: `admin` only.
**Request**: `{ "name": "New Name" }` (same as `CompanyUpdate`).
**Response 200**: `CompanyOut`.
**Errors**: `404 Company not found`, `409 Company name already exists`, `422 name cannot be empty`.

### 2.9 `DELETE /v1/companies/{company_id}`

Permanently delete a company and **all** its associated data: users/profiles, credentials, SIM routing entries, provider mappings, idempotency keys, settings, Moabits source-company cache, and lifecycle-change history references.

**Auth**: `admin` only. Cannot delete own company.

**Response 204** (no body).

**What is deleted** (in order):
1. All `users` (and their `profiles` via cascade) scoped to this company.
2. The `companies` row — the DB `ON DELETE CASCADE` constraints then remove: `company_settings`, `company_provider_credentials`, `company_provider_mappings`, `sim_routing_map`, `idempotency_keys`, `moabits_source_companies`.

**Errors**:
- `400 Cannot delete your own company`
- `404 Company not found`

---

## 3. Tenancy — provider credentials

Implementation: [app/tenancy/routers/credentials.py](app/tenancy/routers/credentials.py),
schemas: [app/tenancy/schemas/credentials.py](app/tenancy/schemas/credentials.py).

A "credential" is the secret (encrypted at rest with `FERNET_KEY`) the
backend uses to call a provider on behalf of the company. Three providers
are supported: `kite`, `tele2`, `moabits`.

The `{provider}` path parameter is enforced as the `Provider` enum →
`kite | tele2 | moabits`.

### 3.1 `GET /v1/companies/me/credentials`

List active credentials for the current company.

**Auth**: `admin` or `manager`.

**Query parameters**:
| Param | Type | Default | Notes |
|---|---|---|---|
| `q` | string | `null` | Filter by provider name or `account_scope` text (case-insensitive `ILIKE`). |

**Response 200**: `CredentialMetadataOut[]` (see § 3.4) — flat JSON array, **not paginated**, ordered by provider name.

### 3.2 `GET /v1/companies/me/credentials/{provider}`

**Auth**: `admin` or `manager`.
**Response 200**: `CredentialMetadataOut`.
**Errors**: `404 Credential not found`.

### 3.3 `POST /v1/companies/me/credentials/{provider}/test`

Live test of a candidate credential set without persisting it.

**Auth**: `admin` or `manager`.

**Request body** ([CredentialUpsertIn](app/tenancy/schemas/credentials.py#L89-L102)):
```json
{
  "credentials": { /* provider-specific — see below */ },
  "account_scope": { /* non-secret metadata */ }
}
```

Per-provider canonical examples (also exported as OpenAPI examples in
[PROVIDER_CREDENTIAL_EXAMPLES](app/tenancy/schemas/credentials.py#L8-L77)):

**Kite** (`POST /credentials/kite/test`):
```json
{
  "credentials": {
    "endpoint": "https://kiteplatform-api.telefonica.com:8010/services/SOAP/GlobalM2M/Inventory/v12/r12",
    "username": "WS_SECURITY_USER",                      // optional, only if Kite provided WS-Sec
    "password": "WS_SECURITY_PASS",                       // optional, only if Kite provided WS-Sec
    "client_cert_pfx_b64": "<base64 of .pfx>",          // REQUIRED
    "client_cert_password": "<pfx password>",            // REQUIRED
    "server_ca_bundle_pem_b64": "<base64 of CA pem>"     // optional
  },
  "account_scope": {
    "environment": "production",
    "end_customer_id": "KITE_END_CUSTOMER_ID"
  }
}
```
Backend normalizes: aliases `pfx_base64`→`client_cert_pfx_b64`,
`pfx_password`→`client_cert_password`, plus several CA-bundle aliases.
Validates that `endpoint` is HTTPS and that username/password come together
and derives `account_scope.cert_expires_at` automatically from the PFX client
certificate, overwriting any value sent by the client.
([_normalized_kite_credentials](app/tenancy/routers/credentials.py#L87-L148)).

**Tele2** (`POST /credentials/tele2/test`):
```json
{
  "credentials": {
    "cobrand_url": "restapi3.jasper.com",  // optional; if omitted defaults to restapi3.jasper.com
    "username": "TELE2_USERNAME",
    "api_key": "TELE2_API_KEY",
    "api_version": "v1"
  },
  "account_scope": {
    "account_id": "TELE2_ACCOUNT_ID",
    "max_tps": 5,
    "environment": "production"
  }
}
```
Backend normalizes `cobrand_url`/`base_url` to `https://<host>` and copies
`account_id` from `account_scope` into `credentials` if missing. `max_tps`
stays in `account_scope` and is injected at runtime by the service layer — it
is not stored inside `credentials_enc`.

**Moabits** (`POST /credentials/moabits/test`):
```json
{
  "credentials": {
    "base_url": "https://www.api.myorion.co",
    "x_api_key": "MOABITS_ORION_X_API_KEY"
  },
  "account_scope": {
    "environment": "production"
  }
}
```

**Response 200** ([CredentialTestOut](app/tenancy/schemas/credentials.py#L105-L108)):
```json
{ "provider": "kite|tele2|moabits", "ok": true, "detail": null }
```
On failure `ok` is `false` and `detail` is a human-readable message
(provider error, missing field, etc.) — the endpoint **always** returns 200
with this envelope (errors do not bubble up).

Live-test probes are backend-only and do not expose secrets to the browser:
Kite calls `getSubscriptions` with `limit=1`, Tele2 calls `/devices` with
`pageSize=1`, and Moabits validates `x_api_key` by exchanging it for a JWT
(`GET /integrity/authorization-token`). No company code is required or stored
as part of credentials — the fixed Moabits parent code is an adapter constant.

### 3.4 `PATCH /v1/companies/me/credentials/{provider}` — partial credential update

Merge-update credentials for a provider. Fields in the request body are merged
into the existing stored credentials; omitted fields are preserved. The merged
result is tested live against the provider before persisting.

**Auth**: `admin` or `manager`.

**Request** ([CredentialPatchIn](app/tenancy/routers/credentials.py#L108-L122)):
```json
{
  "credentials": { /* fields to merge into the stored credential — only changed fields needed */ },
  "account_scope": { /* fields to merge into account_scope — omit to preserve current */ }
}
```
- At least one of `credentials` or `account_scope` must be non-empty → `422` otherwise.
- `credentials` defaults to `{}` (preserve all existing secret fields).
- `account_scope` defaults to `null` (preserve existing account scope without merging).
- For `provider=kite`, `account_scope.cert_expires_at` is always derived from
  `client_cert_pfx_b64`; clients should not ask users to enter it manually.
- For `provider=moabits`, the hyphenated `x-api-key` key is also accepted in
  `credentials` and is normalized to `x_api_key` before merge (so a form field
  named exactly like the Moabits header still works). See
  [_patch_credentials](app/tenancy/routers/credentials.py#L429-L433).

**Manager**: may update any field on any provider credential for their own company.
Cannot create credentials (own-company path requires the credential to exist → `404`).
For `provider=moabits`, only `{"x_api_key": "<value>"}` is accepted in `credentials`
(no other fields, no `account_scope` changes), and the company must have an active
provider mapping. Any other field or combination → `403 Managers can only update Moabits x_api_key`.

**Admin**: can patch any field on any provider, and can create a credential from scratch if
none exists yet for that `(company_id, provider)`.

**Response 200** ([CredentialMetadataOut](app/tenancy/schemas/credentials.py#L79-L86)):
```json
{
  "provider": "kite|tele2|moabits",
  "active": true,
  "rotated_at": "2026-04-01T00:00:00Z",
  "created_at": "2026-04-01T00:00:00Z",
  "account_scope": { /* current merged account scope */ },
  "expiry_status": "valid|expiring|expired|invalid"
}
```
`expiry_status` is computed from `account_scope.cert_expires_at` /
`token_expires_at` / `expires_at` (see [credential_expiry_status](app/tenancy/credential_expiry.py#L20-L47)):
- `valid`: more than 30 days remaining (or no expiry field)
- `expiring`: between 0 and 30 days remaining
- `expired`: past
- `invalid`: present but unparseable

**Errors**:
- `403 Managers can only update Moabits x_api_key` — manager sent extra fields or `account_scope` on Moabits
- `403 Company is not linked to a Moabits company code` — manager PATCH on Moabits without an active mapping
- `404 Credential not found` — manager trying to patch a non-existent credential (managers cannot create)
- `422` with the live-test error message if the merged credential test fails

Frontend flow:
- `GET /v1/companies/me/credentials` is the source of truth for what is already
  active in the current company.
- Missing providers among `kite | tele2 | moabits` can be shown as "add" options (admin only).
- To rotate a full credential set, send all fields in `credentials`; to rotate only one key
  (e.g. `x_api_key`), send only that field.
- `GET /v1/companies/me/credentials/{provider}` returns `200` for edit metadata
  and `404` when the UI should render an empty create form (admin only).
- Secret fields are never returned by any credential endpoint, so forms should
  not prefill secrets.
- **Moabits / manager**: before rendering the credential update form, call
  `GET /v1/companies/me/provider-mappings/moabits`. If it returns `404`, the
  company has no active mapping — show a "contact your admin" message instead of
  the form (the PATCH would return `403`).

### 3.5 `DELETE /v1/companies/me/credentials/{provider}`

Soft-deactivate the active credential record.

**Auth**: `admin`.
**Response 204** (no body).
**Errors**: `404 Credential not found`.

After `DELETE`, the provider disappears from
`GET /v1/companies/me/credentials`; the frontend should show it as missing
again after refresh.

### 3.6 Company-scoped Moabits mapping

Use this when one local company needs to be linked or adjusted to one
specific Moabits company without re-saving the whole Moabits source scope.
Open the discovery endpoint only from the "modify load" action in the UI: it
performs a live Moabits child-company lookup and returns both local companies
from the backend DB and provider companies from Moabits.

```txt
GET    /v1/companies/me/provider-mappings/{provider}          (manager or admin — own company)
GET    /v1/companies/provider-mappings/moabits                (admin — all companies, paginated)
GET    /v1/companies/provider-mappings/moabits/source-companies
GET    /v1/companies/provider-mappings/moabits/discover       (admin — live Moabits lookup)
PUT    /v1/companies/{company_id}/provider-mappings/{provider}
DELETE /v1/companies/{company_id}/provider-mappings/{provider}
```

The `{provider}` path parameter is the `Provider` enum (`kite | tele2 | moabits`),
but only `moabits` is currently supported on these endpoints. Passing `kite` or
`tele2` returns `422 "Only Moabits company mappings are supported"`.

**`GET /v1/companies/me/provider-mappings/moabits`** — `manager` or `admin`

Returns the active Moabits mapping for the authenticated user's company.
Returns `404` if no active mapping exists.
**Response 200**: `CompanyProviderMappingOut` (same shape as the `mapping` object below).

---

Use `GET /v1/companies/provider-mappings/moabits` (**admin** only) for the fast table of
all local companies with their Moabits mapping status. It reads only the backend DB and does
not call Moabits.

**Query parameters**:
| Param | Type | Default | Notes |
|---|---|---|---|
| `page` | int | `1` | Page number (1-based). |
| `size` | int | `50` | Items per page. |
| `q` | string | `null` | Search across company name, `companyCode`, and `companyName` from Moabits. |
| `linked_only` | bool | `false` | When `true`, only returns companies that already have an active mapping. |

**Response 200**: `Page[LocalCompanyProviderMappingOut]`:
```json
{
  "items": [
    {
      "company_id": "00000000-0000-0000-0000-000000000001",
      "company_name": "Acme",
      "mapping": {
        "company_id": "00000000-0000-0000-0000-000000000001",
        "provider": "moabits",
        "companyCode": "A123",
        "companyName": "Acme Energy",
        "clie_id": 42,
        "settings": {},
        "active": true,
        "updated_at": "2026-05-11T00:00:00Z",
        "created_at": "2026-05-11T00:00:00Z"
      }
    },
    {
      "company_id": "00000000-0000-0000-0000-000000000002",
      "company_name": "Beta Corp",
      "mapping": null
    }
  ],
  "total": 2,
  "page": 1,
  "size": 50,
  "pages": 1
}
```

`mapping` is `null` when the company has no active Moabits mapping.

**`GET /v1/companies/provider-mappings/moabits/source-companies`** — `admin`
only.

Returns cached Moabits companies discovered for the admin's Moabits credential
source. This endpoint shows `moabits_source_companies`, not saved local-company
mappings.

The cache is refreshed by `GET /v1/companies/provider-mappings/moabits/discover`:
company codes returned by Moabits become active, and cached codes that no
longer appear become inactive. Existing saved mappings are not deleted, but the
available source company names/codes used for linking may change after
discovery.

**Query parameters**: `page`, `size`, optional `q`, optional
`active_only=true` (default `true`).

**Response 200**: `Page[MoabitsSourceCompanyOut]`:
```json
{
  "items": [
    {
      "source_company_id": "uuid",
      "companyCode": "A123",
      "companyName": "Acme Energy",
      "clie_id": 42,
      "active": true,
      "last_seen_at": "2026-05-11T00:00:00Z",
      "updated_at": "2026-05-11T00:00:00Z",
      "created_at": "2026-05-11T00:00:00Z"
    }
  ],
  "total": 1,
  "page": 1,
  "size": 50,
  "pages": 1
}
```

`source_company_id` is the UUID of the admin company that owns this cached entry (the source credential owner, not the linked company).

**PUT request** (`PUT /v1/companies/{company_id}/provider-mappings/moabits`):
```json
{
  "companyCode": "A123",              // required; also accepted as "provider_company_code"
  "companyName": "Acme Energy",       // optional; also accepted as "provider_company_name"
  "clie_id": 42,                      // optional integer
  "settings": {}                      // optional — arbitrary JSON; defaults to {}
}
```

`PUT` updates only the mapping for `{company_id}`. The `companyCode` is valid
when it is already in the cached `moabits_source_companies` (populated by
`/discover`) **or** when a live Moabits child-companies call resolves it for
the admin's stored X-API-KEY. Validation hits the cache first; the live call
is only made on cache miss. `companyName` and `clie_id`, when omitted in the
request body, are auto-populated from the discovered/cached entry.

**Response 200**:
```json
{
  "company_id": "00000000-0000-0000-0000-000000000001",
  "provider": "moabits",
  "companyCode": "A123",
  "companyName": "Acme Energy",
  "clie_id": 42,
  "settings": {},
  "active": true,
  "updated_at": "2026-05-11T00:00:00Z",
  "created_at": "2026-05-11T00:00:00Z"
}
```

**Discovery response 200**:
```json
{
  "cache_message": "Discovery refreshes the cached Moabits source companies used as link options. Existing saved local-company mappings are not deleted, but available Moabits company codes/names may change.",
  "source_company_codes": ["A123", "B456"],
  "local_companies": [
    {
      "company_id": "00000000-0000-0000-0000-000000000001",
      "company_name": "Acme",
      "mapping": {
        "company_id": "00000000-0000-0000-0000-000000000001",
        "provider": "moabits",
        "companyCode": "A123",
        "companyName": "Acme Energy",
        "clie_id": 42,
        "settings": {},
        "active": true,
        "updated_at": "2026-05-11T00:00:00Z",
        "created_at": "2026-05-11T00:00:00Z"
      }
    }
  ],
  "moabits_companies": [
    {
      "companyCode": "A123",
      "companyName": "Acme Energy",
      "clie_id": 42,
      "selected_in_source": true,
      "linked_companies": [
        {
          "company_id": "00000000-0000-0000-0000-000000000001",
          "company_name": "Acme"
        }
      ]
    }
  ]
}
```

`source_company_codes` comes from the Moabits source companies returned by
discovery, not from saved local-company mappings. Saved links are represented
only in `local_companies[].mapping` and `moabits_companies[].linked_companies`.
The frontend should show `cache_message` before or after running discovery so
admins understand that the cached source-company list is being refreshed.

When `GET /v1/sims?provider=moabits` resolves credentials, the active
company-scoped mapping is required. If no mapping exists the request fails
with `412` — there is no legacy source-scope fallback.

**Errors**: `404 Company not found`, `404 Provider mapping not found`,
`404 Moabits credential not found` for discovery without credentials, `422`
when the Moabits code is not available.

### 3.7 Admin credential scope

These endpoints let platform admins inspect and operate credentials across
companies without depending on `/companies/me/credentials`.

All admin credential responses use `AdminCredentialMetadataOut`, which is
`CredentialMetadataOut` plus `company_id`. Secrets are never returned.

**Auth**: `admin` only.

Endpoints:
- `GET /v1/admin/credentials` — list all active credentials in the platform.
  Accepts optional `q` (matches provider name, `account_scope` text, or owning
  company name). Returns a flat `AdminCredentialMetadataOut[]` (not paginated).
- `GET /v1/admin/companies/{company_id}/credentials` — list active credentials
  for one company. Accepts optional `q` (provider or `account_scope`). Returns
  `AdminCredentialMetadataOut[]` (not paginated).
- `GET /v1/admin/companies/{company_id}/credentials/{provider}` — get one active
  credential metadata record; `404` when missing.
- `POST /v1/admin/companies/{company_id}/credentials/{provider}/test` — live
  test candidate credentials for that company without persisting. Same body and
  response shape as `POST /v1/companies/me/credentials/{provider}/test` (see § 3.3).
- `POST /v1/admin/companies/{company_id}/credentials/{provider}/probe` — live
  smoke test the already-stored active credential for that company/provider
  without sending secrets in the request body. Calls the provider listing with
  `limit=1`. Success returns
  `{ "provider": "kite|tele2|moabits", "ok": true, "detail": "...", "sample_count": 0|1 }`.
  Failures bubble as `application/problem+json` using the same codes as SIM
  listings: `tenant.credentials_missing`, `subscription.listing_precondition_failed`,
  `provider.auth_failed`, `provider.protocol_error`, `provider.unavailable`,
  or `provider.rate_limited`.
- `PATCH /v1/admin/companies/{company_id}/credentials/{provider}` — update or
  create the active credential for that company/provider. Same merge semantics
  as § 3.4; admin scope always allows create (no manager-only restrictions).
- `DELETE /v1/admin/companies/{company_id}/credentials/{provider}` — soft
  deactivate the active credential. `404` when no active credential exists.

`{company_id}` is validated up-front on every endpoint in this group — an
unknown company returns `404 Company not found` before any credential lookup
runs.

---

## 4. SIMs

Implementation: [app/subscriptions/routers/sims.py](app/subscriptions/routers/sims.py),
schemas: [app/subscriptions/schemas/sim.py](app/subscriptions/schemas/sim.py),
domain: [app/subscriptions/domain.py](app/subscriptions/domain.py).

All endpoints are scoped to the caller's company through the JWT profile.

### 4.1 `GET /v1/sims`

Two modes:

1. **Provider-scoped** (`?provider=kite|tele2|moabits`) — calls the
   adapter's native listing. Lazy-populates the local `sim_routing_map`.
2. **Global** (no `provider`) — fans out to searchable providers, requests
   provider summary pages, and lazy-populates `sim_routing_map`.

**Auth**: any authenticated tenant user.

**Query parameters** ([list_sims signature](app/subscriptions/routers/sims.py#L779-L822)):

| Param | Type | Default | Notes |
|---|---|---|---|
| `cursor` | string | `null` | Opaque pagination cursor returned in `next_cursor`. |
| `limit` | int | `50` | Provider-dependent upper bound; global path clamps to `[1, 500]` and distributes that total budget across queried providers. |
| `provider` | enum | `null` | `kite \| tele2 \| moabits`. Omit for global listing. |
| `status` | string | `null` | Raw provider status value. Provider-scoped only. |
| `modified_since` | string | `null` | `yyyy-MM-ddTHH:mm:ssZ`. **Required** when `provider=tele2` and no cursor. Ignored by Moabits. |
| `modified_till` | string | `null` | Same format as above. |
| `iccid` | string | `null` | Provider-scoped filter. |
| `imsi` | string | `null` | Provider-scoped filter. |
| `msisdn` | string | `null` | Provider-scoped filter. |
| `custom` | string[] | `[]` | Repeated query param. Each item is `key=value` or `key:value`. Provider-scoped filter. |

**Response 200** ([SimListOut](app/subscriptions/schemas/sim.py#L234-L268)):
```json
{
  "items": [ /* SubscriptionOut[] — see below */ ],
  "next_cursor": "string|null",
  "total": null,
  "partial": false,
  "failed_providers": [
    { "provider": "moabits", "code": "provider.unsupported_operation", "title": "Operation not supported by this provider" }
  ],
  "provider_statuses": [
    { "provider": "kite",    "status": "ok",    "count": 1, "code": null,                             "title": null },
    { "provider": "tele2",   "status": "ok",    "count": 0, "code": null,                             "title": null },
    { "provider": "moabits", "status": "error", "count": 0, "code": "provider.unsupported_operation", "title": "Operation not supported by this provider" }
  ]
}
```

`provider_statuses` ([ProviderStatusOut](app/subscriptions/schemas/sim.py#L210-L232)) is present on every global listing and ICCID search response. Use it to show a per-provider breakdown in the UI instead of scanning `failed_providers`. Fields:

| Field | Values | Notes |
|---|---|---|
| `provider` | `kite\|tele2\|moabits` | |
| `status` | `ok\|partial\|error\|not_queried` | `not_queried` = skipped (cursor-based pagination excluded it, or routing map already resolved the SIM to one provider) |
| `count` | int | SIMs contributed by this provider in this page |
| `code` | string\|null | Machine-readable error code when `status=error` |
| `title` | string\|null | Human-readable summary when `status=error` |

**`SubscriptionOut`** ([app/subscriptions/schemas/sim.py:29-176](app/subscriptions/schemas/sim.py#L29-L176)):
```json
{
  "iccid": "8988216716970004975",
  "msisdn": "882351697004975",
  "imsi": "901161697004975",
  "status": "ACTIVATED",                       // raw provider status value
  "provider": "tele2",
  "company_id": "uuid",
  "activated_at": "2016-06-29T00:21:33.339000Z",
  "updated_at":  "2016-07-06T22:04:04.380000Z",
  "detail_level": "summary|detail",            // "summary" = lighter listing row, "detail" = enriched
  "provider_fields": { /* raw, provider-specific keys */ },
  "normalized": {
    "identity":   { "imei": "...", "alias": "...", "eid": "...", "euiccid": "...", "sim_profile_id": "..." },
    "status":     { "label": "Activated", "group": "active_like", "group_label": "Active-like", "source": "provider", "last_changed_at": "..." },
    "plan":       { "name": "...", "code": "...", "id": "...", "communication_plan": "...", "apn": "...", "apns": [...], "started_at": "...", "expires_at": "..." },
    "customer":   { "name": "...", "id": "...", "company_code": "...", "account_id": "..." },
    "network":    { "operator": "...", "country": "...", "rat_type": "...", "last_network": "...", "ip_address": "...", "ipv6_address": "...", "fixed_ip_address": "...", "fixed_ipv6_address": "...", "static_ips": [...], "additional_static_ips": [...], "sgsn_ip": "...", "ggsn_ip": "...", "last_traffic_at": "...", "first_lu_at": "...", "last_lu_at": "...", "first_cdr_at": "...", "last_cdr_at": "...", "gprs_status": "...", "ip_status": "...", "location": "..." },
    "hardware":   { "sim_model": "...", "module_manufacturer": "...", "module_model": "...", "device_id": "...", "modem_id": "...", "imei_last_changed_at": "...", "shipped_at": "..." },
    "services":   { "active": ["data","sms"], "basic": "...", "supplementary": "...", "data_service": true, "sms_service": true },
    "limits":     { "data": 5000, "data_unit": "mb", "sms": 100, "daily": { "<metric>": { "limit": ..., "value": ..., "threshold_reached": ..., "traffic_cut": ..., "enabled": ... } }, "monthly": { ... } },
    "dates":      { "added_at": "...", "provisioned_at": "..." },
    "custom_fields": { /* keys starting with account_custom_, operator_custom_, customer_custom_, custom_field_ */ }
  }
}
```

**Frontend guidance**: prefer the `normalized` blocks for UI; use
`provider_fields` only for advanced/native views. Any block value can be
`null` if the provider didn't include that data — handle accordingly.
Fields already present at the top level (`iccid`, `msisdn`, `imsi`, `status`,
`provider`, `activated_at`, `updated_at`) are intentionally not duplicated
inside `normalized`; use the top-level values as the source of truth.
`normalized.network.ip_address` is a current/session address when provided;
fixed/static addresses stay in `fixed_ip_address`, `fixed_ipv6_address`, or
`static_ips`.

**Provider quirks the frontend must surface**:
- Tele2 provider-scoped listing without `modified_since` and without a
  cursor returns `400` with the **provider-style** body
  (the only place this body shape is used):
  ```json
  { "errorMessage": "ModifiedSince is required.", "errorCode": "10000003" }
  ```
  See [_tele2_missing_modified_since_response](app/subscriptions/routers/sims.py#L468-L475).
- Moabits provider-scoped listing returns `412 subscription.listing_precondition_failed`
  if no Moabits company code mapping is configured (see § 3.7).
- Global listing may return `not_queried` in `provider_statuses` when a small
  `limit` budget leaves one or more providers for the returned global cursor.
- **Global ICCID search** (`?iccid=<value>` with no other filters): special fast path — the
  backend checks the routing map first (one provider call if known), otherwise fans out to
  providers that support ICCID lookup. Kite and Tele2 use native ICCID search; Moabits loads
  `simList/{company_code}` and filters the returned array locally. If a provider fails, the
  response is `200` with `partial: true`.
- Global listing with **any other filter** (`status`, `modified_since`, `modified_till`, `imsi`,
  `msisdn`, `custom`) — or with `iccid` combined with those — returns
  `409 provider.unsupported_operation`. Use `?provider=<name>` for one
  provider, or `POST /v1/sims/search` for one merged table with
  provider-specific filters.
- A global listing where one provider failed returns `200` with `partial: true` and
  the error in `failed_providers` (other items are still present in `items`).

### 4.2 `POST /v1/sims/search` — merged table with provider-specific filters

Use this when the UI needs one table but each source needs its own native
filters. The backend remains the integration layer: it validates provider
keys, calls each provider adapter with that provider's filters, merges results
into `SimListOut`, and returns per-provider status metadata.

`status` / `statuses` values here are provider-native. Do not send a global
lifecycle enum.

**Request body**:
```json
{
  "limit": 50,
  "cursor": null,
  "common": {
    "iccid": null,
    "imsi": null,
    "msisdn": null,
    "modified_since": "2026-04-18T17:31:34Z",
    "modified_till": null,
    "custom": {}
  },
  "providers": {
    "tele2":   { "statuses": ["ACTIVATED", "TEST_READY"] },
    "kite":    { "statuses": ["ACTIVE", "TEST"] },
    "moabits": { "status": "Suspended" }
  }
}
```

If `providers` is empty or omitted, the backend queries all providers with the
`common` filters. A provider block can override common filters and may also set
its own `cursor`, `limit`, `custom`, `status`, and `statuses` fields.

- Use `status` as shorthand for one provider-native value.
- Use `statuses` when the UI needs multiple native values from the same
  provider, e.g. Tele2 `ACTIVATED` + `TEST_READY`.
- When `statuses` has multiple values, the backend performs one provider query
  per status and encodes any returned per-status cursors inside the response
  `next_cursor`. The frontend should pass `next_cursor` back unchanged.

**Response 200**: same `SimListOut` shape as `GET /v1/sims`.

**Architectural rule**: `normalized.status.group` is only a coarse UI grouping
such as `active_like`; the primary visible status should be
`normalized.status.label` plus provider context, and actions should come from
`GET /v1/providers/{provider}/capabilities`.

### 4.3 `GET /v1/sims/{iccid}`

**Auth**: any authenticated tenant user.
**Response 200**: `SubscriptionOut`.

**Lookup flow** (lazy discovery):
1. Look up `iccid` in `sim_routing_map`. Hit → call the resolved provider's `get_subscription` directly.
2. Miss → fan out to every registered provider whose adapter declares `supports_list_filter("iccid")` (today: kite, tele2, moabits) in parallel with `limit=1` and an `iccid` filter. The first provider returning a match populates `sim_routing_map` and the response is served from that prefetched subscription (no extra round-trip).
3. Still no match → return `404 subscription.not_found` and record a short-lived (~60s) negative cache so a repeated query for the same unknown ICCID short-circuits without hitting the providers again.

Provider-level errors during the fan-out (timeouts, 5xx, credential issues) are logged and treated as "miss from that provider" — discovery succeeds if any provider returns a match.

`GET /v1/sims/{iccid}/usage` and `GET /v1/sims/{iccid}/presence` share the same resolution flow (routing map → fan-out → 404). Write endpoints (`PUT .../status`, `POST .../purge`) remain strict and return `404` immediately on routing-map miss — they require the SIM to be known before performing a mutation.

**Errors**:
- `404 subscription.not_found` — neither the routing map nor any provider that supports ICCID filtering returned a match. The detail message explains how to bootstrap (e.g. for a SIM on a provider whose listing cannot filter by ICCID).
- `412 tenant.credentials_missing` — no active credential for this SIM's provider.

### 4.4 `GET /v1/sims/{iccid}/usage`

**Auth**: any authenticated tenant user.

**Query parameters**:
| Param | Type | Default | Notes |
|---|---|---|---|
| `start_date` | datetime | `null` | ISO-8601. Provider-defined default if omitted. |
| `end_date` | datetime | `null` | ISO-8601. |
| `metrics` | string | `null` | Comma-separated list, e.g. `data,sms`. |

**Response 200** ([UsageOut](app/subscriptions/schemas/sim.py#L185-L195)):
```json
{
  "iccid": "...",
  "period_start": "2026-04-01T00:00:00Z",
  "period_end":   "2026-04-30T23:59:59Z",
  "data_used_bytes": "1234567890",       // Decimal as string (JSON safe)
  "sms_count": 42,
  "voice_seconds": 0,
  "provider_metrics": { /* opaque */ },
  "usage_metrics": [
    { "metric_type": "data_in", "usage": "1024", "unit": "MB" }
  ]
}
```

### 4.5 `GET /v1/sims/{iccid}/presence`

**Auth**: any authenticated tenant user.

**Response 200** ([PresenceOut](app/subscriptions/schemas/sim.py#L198-L207)):
```json
{
  "iccid": "...",
  "state": "online|offline|unknown",
  "ip_address": "string|null",
  "country_code": "string|null",
  "rat_type": "string|null",
  "network_name": "string|null",
  "last_seen_at": "2026-04-01T00:00:00Z|null"
}
```

### 4.6 `PUT /v1/sims/{iccid}/status` — lifecycle write

**Auth**: `admin` only.

**Headers**:
- `Authorization: Bearer ...`
- `Idempotency-Key: <unique-string>` — **required**. Same `(company_id, key)`
  within 24h is short-circuited and returns `204` without re-calling the
  provider.

**Request body** ([StatusChangeIn](app/subscriptions/schemas/sim.py#L238-L245)):
```json
{
  "target": "ACTIVATED|PURGED|active|suspended|...", // provider-native target status
  "data_service": true,                            // optional, Moabits only
  "sms_service": false                             // optional, Moabits only
}
```

**Response 204** (no body).

**Per-provider valid `target` values** (see [GET /v1/providers/{provider}/capabilities](#6-providers--capabilities) for the live list, examples below):
- Kite: `ACTIVE, TEST, ACTIVATION_READY, ACTIVATION_PENDANT, INACTIVE_NEW`
- Tele2: `ACTIVATED, TEST_READY, ACTIVATION_READY, DEACTIVATED, INVENTORY, PURGED, RETIRED, REPLACED`
- Moabits: `active, suspended` (and at least one of `data_service` or `sms_service` must be `true`)

**Errors**:
- `400 request.idempotency_key_required` — header missing
- `403` — caller is not admin
- `409 provider.unsupported_operation` — feature flag `LIFECYCLE_WRITES_ENABLED` is off, or provider does not accept this transition
- `404 subscription.not_found` — not in routing map
- `409 provider.unsupported_operation` — provider does not accept this transition
- `412 tenant.credentials_missing`
- `429 provider.rate_limited` (with `extra.retry_after` when known)
- `502/503` — provider auth/protocol/availability issues (see § 8)

### 4.7 `POST /v1/sims/{iccid}/purge`

**Auth**: `admin` only.

**No request body.**

---

#### What purge means — per provider

Purge is **not the same operation** on all providers. The frontend should use
a different confirmation dialog depending on the SIM's `provider` field:

| Provider | What actually happens | Reversible? |
|---|---|---|
| `kite` | Calls Kite `networkReset`. Resets network data (session, IP). **Does not change the SIM's lifecycle status.** | Yes — SIM stays in same state |
| `tele2` | Transitions the SIM to the `PURGED` status via `Edit Device Details`. Terminal state. | No |
| `moabits` | Calls Orion `PUT /api/sim/purge/` and verifies `info.purged=true`. Permanent removal. | No |

The `provider` field comes from `GET /v1/sims/{iccid}` → `subscription.provider`.

---

#### Pre-check before showing the button

Call `GET /v1/providers/{provider}/capabilities` and inspect
`capabilities.purge.status`:

| Value | What to show |
|---|---|
| `supported` | Show the button |
| `requires_feature_flag` | Hide the button (backend writes are disabled by ops) |

See [§ 6.1](#61-get-v1providerprovidercapabilities) for the full capabilities response shape.

---

#### Headers

| Header | Required | Notes |
|---|---|---|
| `Authorization: Bearer <jwt>` | yes | Admin role required |
| `Idempotency-Key: <string>` | **yes** | Any unique string per logical action. UUID recommended. 24 h window scoped to `(company_id, key)`. |
| `X-Request-ID` | recommended | Correlate with server logs on error |

---

#### Idempotency key behaviour

The backend uses the `(company_id, Idempotency-Key)` pair to deduplicate
calls within a 24-hour window. Rules the frontend must follow:

1. **New action → new key.** Generate a fresh UUID each time the user triggers
   a purge on a different SIM or retries after a network error (key was not
   claimed).
2. **Same key on retry.** If the request failed before the provider was reached
   (network timeout, 5xx before `204` was received), retry with the **same key**
   — the backend will call the provider again safely.
3. **Key already processed.** If `204` was returned once, a second request with
   the same key returns `204` immediately without calling the provider again
   (idempotency replay). You will never get a `409 duplicate` for this endpoint.
4. **Key expiry.** After 24 h the key is no longer tracked. A new request with
   the same key would trigger the provider again.

---

#### Response `204`

No body. The purge was executed (or replayed). The action has been audited
server-side regardless of whether this was a first call or an idempotency replay.

---

#### Error matrix

| HTTP | `code` | When | Frontend action |
|---|---|---|---|
| `400` | `request.idempotency_key_required` | `Idempotency-Key` header missing | Show "retry" with a generated key |
| `403` | _(FastAPI default `{"detail":"..."}`)_ | Caller is not `admin` | Hide button for non-admin roles |
| `404` | `subscription.not_found` | Purge target unknown (routing-map miss; purge does not trigger lazy discovery) | Show "SIM not found. Open the SIM detail or refresh the list to populate routing first." |
| `409` | `provider.unsupported_operation` | `LIFECYCLE_WRITES_ENABLED` is off in backend config | Hide purge UI; lifecycle writes are disabled by ops |
| `412` | `tenant.credentials_missing` | Provider has no active credential for this company | Show "Provider credentials missing. Contact admin." |
| `422` | `provider.validation_error` | Provider rejected the call (e.g. Moabits did not confirm `info.purged=true`) | Show `detail`. Log `instance` for support. |
| `429` | `provider.rate_limited` | Provider throttled the request | Retry after `extra.retry_after` seconds (if present); otherwise back off |
| `502` | `provider.auth_failed` | Backend credentials expired at the provider | Show "Provider authentication failed. Contact admin." |
| `502` | `provider.protocol_error` | Unexpected provider response | Show generic error. Log `instance`. |
| `503` | `provider.unavailable` | Provider network error or timeout | Show "Provider temporarily unavailable. Try again shortly." |

All errors except `400`/`403` use the RFC 7807 envelope — branch on `code`,
show `detail` as the message, log `instance` for support tickets.

---

#### Full request example

```http
POST /v1/sims/8988216716970004975/purge HTTP/1.1
Authorization: Bearer <access_token>
Idempotency-Key: 7f3a1c22-4b9e-4f8d-a1e0-3c2b5d6e7f8a
X-Request-ID: req-abc123
Content-Length: 0
```

Success:
```http
HTTP/1.1 204 No Content
X-Request-ID: req-abc123
X-API-Version: v1
```

Error (feature flag off):
```http
HTTP/1.1 409 Conflict
Content-Type: application/problem+json

{
  "type": "https://api.example.com/errors/provider.unsupported_operation",
  "title": "Operation not supported by this provider",
  "status": 409,
  "code": "provider.unsupported_operation",
  "detail": "Lifecycle write operations are disabled by feature flag",
  "instance": "req-abc123"
}
```

### 4.8 `POST /v1/sims/import` — bootstrap routing map

Upserts `(iccid, provider) → company_id` rows so single-SIM endpoints work
before a full provider listing has run.

**Auth**: any authenticated tenant user.

**Request body** ([SimImportIn](app/subscriptions/schemas/sim.py#L253-L254)):
```json
{
  "sims": [
    { "iccid": "8988...001", "provider": "kite" },
    { "iccid": "8988...002", "provider": "tele2" }
  ]
}
```

**Response 200** ([SimImportOut](app/subscriptions/schemas/sim.py#L257-L258)):
```json
{ "imported": 2 }
```

**Errors**:
- `400 Unknown provider(s): [<list>]. Valid: ["kite","moabits","tele2"]`

### 4.9 `POST /v1/sims/details` — batch detail lookup

For multi-provider filter use cases (frontend pastes/uploads a list of ICCIDs,
possibly from mixed providers, and needs current state for each). The backend
resolves each ICCID to its owning provider via `sim_routing_map`, fans out in
parallel to each provider, and returns a per-ICCID response. Each provider call
respects its own rate limiter (Tele2 TPS budget is preserved automatically).

**Auth**: any authenticated tenant user.

**Implementation**: ADR-012 §3 — implemented in backend Fase C.

**Request body**:
```json
{
  "iccids":    ["8946...01", "8946...02", "8934...03"],
  "providers": ["kite", "tele2"]
}
```

| Field | Type | Required | Notes |
|---|---|---|---|
| `iccids` | string[] | yes | 1 to 200 ICCIDs. Above 200 → `413 request.batch_too_large`. |
| `providers` | enum[] | no | Optional filter `kite\|tele2\|moabits`. If omitted, no filter. ICCIDs whose routing-resolved provider is not in this list go to `filtered_out`. |

**Response 200** (per-ICCID status — never all-or-nothing):
```json
{
  "results": {
    "8946...01": { "provider": "kite",    "status": "ok",           "data": { /* SubscriptionOut */ } },
    "8946...02": { "provider": "tele2",   "status": "not_found",    "error": { "code": "subscription.not_found", "detail": "provider returned 404" } },
    "8946...03": { "provider": "moabits", "status": "timeout",      "error": { "code": "provider.unavailable", "detail": "provider timeout after 8s" } },
    "8946...04": { "provider": "tele2",   "status": "rate_limited", "error": { "code": "provider.rate_limited", "retry_after": 12 } }
  },
  "summary":      { "ok": 1, "not_found": 1, "timeout": 1, "rate_limited": 1, "error": 0, "total": 4 },
  "unresolved":   ["8946...05"],
  "filtered_out": ["8946...06"]
}
```

| Field | Notes |
|---|---|
| `results[iccid].provider` | Resolved via `sim_routing_map`. |
| `results[iccid].status` | `ok \| not_found \| timeout \| error \| rate_limited` |
| `results[iccid].data` | `SubscriptionOut` (see § 4.1) — present only when `status=ok`. |
| `results[iccid].error` | `{code, detail, retry_after?}` — present only when `status != ok`. |
| `unresolved` | ICCIDs not in routing map after cascade fallback (`sim_routing_prefix_map` → cross-provider fan-out). Frontend can prompt "refresh routing". |
| `filtered_out` | ICCIDs filtered out by the `providers` request param. |

**Errors** (response-level — different from per-ICCID errors above):
- `400 request.validation_error` — empty `iccids` or invalid enum in `providers`.
- `413 request.batch_too_large` — more than 200 ICCIDs in one request. Use `POST /v1/sims/export` for massive lookups.

**Frontend guidance**:
- Prefer this endpoint over N parallel `GET /v1/sims/{iccid}` calls when paginating ≥10 SIMs.
- Render rows by `status` (`ok` → data card; `timeout` → retry button; `not_found` → grey out; `rate_limited` → wait + retry after `retry_after`).
- TanStack Query: key the `useQuery` on the sorted ICCID list so re-renders don't refire.

### 4.10 `POST /v1/sims/export` — async massive export

For exports of thousands of SIMs with full detail (CSV or JSON). Queues a
background job and returns immediately with a `job_id`. Poll
`GET /v1/jobs/{job_id}` (see § 5.1) for status and the result URL.

**Auth**: `admin` only.

**Implementation**: ADR-012 §4 — pending implementation (fase D of the migration plan).

**Request body**:
```json
{
  "providers": ["tele2"],
  "format":    "csv",
  "fields":    ["iccid", "msisdn", "status", "normalized.status.label"]
}
```

| Field | Type | Required | Notes |
|---|---|---|---|
| `providers` | enum[] | no | Filter by provider. If omitted, all providers the tenant has credentials for. |
| `format` | enum | yes | `csv \| json`. |
| `fields` | string[] | yes | List of `SubscriptionOut` field paths to include (top-level or dotted, e.g. `normalized.status.label`). |

**Response 202**:
```json
{
  "job_id":     "exp_01HXY1Z2W3Q4R5S6T7U8V9",
  "status_url": "/v1/jobs/exp_01HXY1Z2W3Q4R5S6T7U8V9"
}
```

**Errors**:
- `403` — caller is not `admin`.
- `409 sync.already_running` — an export with identical params for the same tenant is already in flight.

**Frontend guidance**: poll `GET /v1/jobs/{job_id}` every 3–5s with
`useQuery({ refetchInterval })`. Stop polling when `status ∈ {done, failed}`. On
`done`, redirect/download from `result_url` (signed URL, TTL 24h by default).

---

## 5. Async jobs & sync

Implementation: `app/sync/` (ADR-012 fase B implemented; export job producer/worker from fase D pending). Backed by Arq + Redis.
Jobs are persisted in `sync_jobs`; the API reads from there to answer
`GET /v1/jobs/{id}` and `GET /v1/sync/status` regardless of whether the job
is still in flight in Redis.

### 5.1 `GET /v1/jobs/{job_id}`

Polls the status of any async job (export, routing sync).

**Auth**: same tenant as the job's owner, or `admin`.

**Response 200**:
```json
{
  "job_id":            "exp_01HXY...",
  "kind":              "export|routing_sync",
  "provider":          "tele2",                              // null for cross-provider exports
  "status":            "pending|running|done|failed",
  "progress":          { "done": 4521, "total": 12340 },     // total may be null if provider does not expose a count
  "created_at":        "2026-05-25T14:00:00Z",
  "started_at":        "2026-05-25T14:00:02Z",
  "finished_at":       null,
  "result_url":        null,                                 // signed URL when status=done (export only)
  "result_expires_at": null,                                 // ISO-8601 when result_url expires
  "error":             null                                  // {code, detail} when status=failed
}
```

**Errors**:
- `404 job.not_found` — unknown `job_id`, or the job belongs to another tenant.
- `410 export.result_expired` — job is `done` but `result_url` TTL expired (default 24h).

**Frontend guidance**: stop polling when `status ∈ {done, failed}`. Render the
progress bar from `done/total` when `total != null`; otherwise show an
indeterminate spinner with the count.

### 5.2 `GET /v1/sync/status`

Returns the freshness of the routing map per provider and the most recent sync
per tenant. Useful for the dashboard "last synced" badges.

**Auth**: any authenticated tenant user.

**Response 200**:
```json
{
  "providers": {
    "kite":    { "last_sync_at": "2026-05-25T02:01:43Z", "status": "done",    "last_job_id": "sync_01H...", "iccids_seen": 12420 },
    "tele2":   { "last_sync_at": "2026-05-25T02:15:18Z", "status": "done",    "last_job_id": "sync_01H...", "iccids_seen": 89711 },
    "moabits": { "last_sync_at": "2026-05-24T02:30:09Z", "status": "failed",  "last_job_id": "sync_01H...", "iccids_seen": 0, "error": { "code": "provider.unavailable", "detail": "..." } }
  },
  "in_flight": [
    { "job_id": "sync_01H...", "provider": "moabits", "started_at": "2026-05-25T13:42:01Z" }
  ]
}
```

**Frontend guidance**: show a "Last synced X hours ago" badge per provider in
the SIMs view header. Highlight `failed` syncs and offer a one-click
`POST /v1/sync/trigger` (admin only).

### 5.3 `POST /v1/sync/trigger`

Forces an out-of-cycle routing sync for one provider. Cron-scheduled syncs run
at 02:00 UTC by default (configurable via `SYNC_CRON_EXPR`).

**Auth**: `admin` only.

**Query parameters**:

| Param | Type | Required | Notes |
|---|---|---|---|
| `provider` | enum | yes | `kite \| tele2 \| moabits` |

**Response 202**:
```json
{
  "job_id":     "sync_01HXY...",
  "status_url": "/v1/jobs/sync_01HXY..."
}
```

**Errors**:
- `403` — caller is not `admin`.
- `409 sync.already_running` — a sync for this `(tenant, provider)` is already pending or running. Inspect `in_flight` from `GET /v1/sync/status` to see the active job.

---

## 6. Providers — capabilities

Implementation: [app/providers/routers.py](app/providers/routers.py),
schemas: [app/providers/schemas.py](app/providers/schemas.py).

The frontend should call this **before** rendering write actions to know
whether the action is supported and which targets are valid.

### 6.1 `GET /v1/providers/{provider}/capabilities`

**Auth**: any authenticated profile (no tenant scope required — the answer
does not depend on the caller's company).

**Response 200** ([ProviderCapabilitiesOut](app/providers/schemas.py#L19-L21)):
```json
{
  "provider": "kite",
  "capabilities": {
    "list_subscriptions":       { "status": "supported",                 "reason": null, "targets": [] },
    "get_subscription":         { "status": "supported",                 "reason": null, "targets": [] },
    "get_usage":                { "status": "supported",                 "reason": null, "targets": [] },
    "get_presence":             { "status": "supported",                 "reason": null, "targets": [] },
    "set_administrative_status":{ "status": "supported|requires_feature_flag",
                                  "reason": "Kite modifySubscription supports only ...",
                                  "targets": ["ACTIVE","TEST","ACTIVATION_READY","ACTIVATION_PENDANT","INACTIVE_NEW"] },
    "purge":                    { "status": "supported|requires_feature_flag", "reason": "...", "targets": [] },
    "status_history":           { "status": "supported|not_supported",   "reason": null, "targets": [] },
    "aggregated_usage":         { "status": "not_supported",             "reason": "...", "targets": [] },
    "plan_catalog":             { "status": "not_supported",             "reason": "...", "targets": [] },
    "quota_management":         { "status": "not_supported",             "reason": "...", "targets": [] }
  }
}
```

`status` enum:
- `supported` — call away
- `not_supported` — endpoint exists but provider can't fulfill it
- `requires_feature_flag` — backend `LIFECYCLE_WRITES_ENABLED` is off
- `requires_confirmation` — reserved (not currently emitted)

`targets` is the list of provider-native values accepted by
`set_administrative_status` (frontend should drive a dropdown from this).

---

## 7. Health

| Method | Path | Notes |
|---|---|---|
| `GET` | `/health` | Returns `{ "status": "ok" }`. No DB call. |
| `GET` | `/ready`  | Returns `{ "status": "ready" }` only after a `SELECT 1` succeeds. Use as readiness probe. |

These endpoints are unauthenticated and **not** under `/v1`.

---

## 8. Domain enums

### 8.1 Status values

`Subscription.status` is a provider-native string. Examples:

```
Kite: ACTIVE | TEST | INACTIVE_NEW | ACTIVATION_READY | ACTIVATION_PENDANT
Tele2: ACTIVATED | TEST_READY | PURGED | DEACTIVATED | INVENTORY | REPLACED | RETIRED | ACTIVATION_READY
Moabits: Active | Suspended | active | suspended
```

For writes, use the `targets` advertised by `GET /v1/providers/{provider}/capabilities`.

### 8.2 `Provider`

Source: [app/providers/base.py:27-30](app/providers/base.py#L27-L30).

```
kite | tele2 | moabits
```

### 8.3 `AppRole`

Source: [app/identity/models/profile.py:12-16](app/identity/models/profile.py#L12-L16).

```
public | admin | manager | member
```

### 8.4 `CapabilityStatus`

```
supported | not_supported | requires_feature_flag | requires_confirmation
```

### 8.5 `CredentialExpiryStatus`

```
valid | expiring | expired | invalid
```

### 8.6 `ConnectivityState` (presence)

```
online | offline | unknown
```

---

## 9. Error envelope (RFC 7807)

Every business error returns `application/problem+json` with this shape
([app/main.py:60-81](app/main.py#L60-L81)):

```json
{
  "type": "https://api.example.com/errors/<code>",
  "title": "Human-readable title",
  "status": 412,
  "code": "subscription.listing_precondition_failed",
  "detail": "Routing map is empty for this tenant. ...",
  "instance": "<X-Request-ID>",
  "...": "any extra fields the error attached"
}
```

Frontend handling rules:
- Use `code` (machine-readable) for branching, not `title`.
- Show `detail` to the user as the action message.
- Log `instance` (= `X-Request-ID`) so support can find the server log line.

### Known `code` values

Source: [app/shared/errors.py](app/shared/errors.py).

| Code | HTTP | Title | Surfaces from |
|---|---|---|---|
| `subscription.not_found` | 404 | Subscription not found | `GET /sims/{iccid}*` after both the routing map and cross-provider fan-out miss; lifecycle writes (`PUT .../status`, `POST .../purge`) on routing-map miss (writes do not trigger fan-out) |
| `subscription.invalid_iccid` | 400 | Invalid ICCID format | provider adapters |
| `subscription.partial_result` | 207 | Partial result — some providers failed | currently surfaced as `200` with `partial: true` in `SimListOut` |
| `subscription.listing_precondition_failed` | 412 | Listing precondition failed | global `GET /sims` with empty routing map; provider-scoped Moabits listing without company codes |
| `provider.unavailable` | 503 | Provider temporarily unavailable | adapter network/timeouts; may include `Retry-After` HTTP header + `extra.retry_after` body field |
| `provider.rate_limited` | 429 | Provider rate limit reached | adapter; may include `Retry-After` HTTP header + `extra.retry_after` body field if provider supplied it |
| `provider.auth_failed` | 502 | Provider authentication failed | bad/expired upstream credentials |
| `provider.protocol_error` | 502 | Unexpected response from provider | parsing/contract violation |
| `provider.resource_not_found` | 404 | Resource not found on provider | provider-side 404; may include `extra.provider_request_id`, `provider_error_code`, `provider_error_message` |
| `provider.validation_error` | 422 | Provider validation error | provider rejected the call (e.g. invalid target status) |
| `provider.forbidden` | 403 | Provider forbidden | provider denied access |
| `provider.unsupported_operation` | 409 | Operation not supported by this provider | global listing with filters; transitions a provider doesn't accept |
| `tenant.credentials_missing` | 412 | No active provider credentials for this company | `/sims*` when the SIM's provider has no active credential |
| `auth.forbidden` | 403 | Insufficient permissions for this operation | reserved |
| `request.idempotency_key_required` | 400 | Idempotency-Key header is required for this operation | lifecycle writes without the header |
| `request.batch_too_large` | 413 | Batch size exceeds the limit | `POST /v1/sims/details` with > 200 ICCIDs (ADR-012) |
| `request.validation_error` | 400 | Request body or params failed validation | `POST /v1/sims/details` (empty `iccids`, invalid `providers` enum); other endpoints with explicit validation failures |
| `job.not_found` | 404 | Async job not found or not owned by tenant | `GET /v1/jobs/{job_id}` (ADR-012) |
| `sync.already_running` | 409 | A job with the same scope is already pending or running | `POST /v1/sync/trigger`, `POST /v1/sims/export` (ADR-012) |
| `export.result_expired` | 410 | Job is done but the result artifact TTL has expired | `GET /v1/jobs/{job_id}` for exports past `result_expires_at` (ADR-012) |
| `internal_error` | 500 | Internal server error | unhandled exception fallback |

### Other error envelopes

Three endpoints intentionally do **not** use problem+json:

- `HTTPException`-based errors raised directly by routers (auth/users/companies/credentials)
  return `FastAPI`'s default `{"detail": "<string-or-object>"}` body.
  This is the standard format for `400/401/403/404/409/422` returned by
  endpoints in §§ 1–3.
- `Pydantic` validation errors return FastAPI's default 422 body
  `{"detail":[{"loc":[...], "msg":"...", "type":"..."}]}`.
- The Tele2 missing-`modified_since` 400 returns `{"errorMessage":"...","errorCode":"..."}`
  (mirrors Tele2's own format — see [app/subscriptions/routers/sims.py:468-475](app/subscriptions/routers/sims.py#L468-L475)).

When integrating, the frontend should branch on:
1. HTTP status code first.
2. Then `code` if `application/problem+json`, else `detail` (string or array).

---

## Quick recipes for frontend flows

### Sign up & first login (new tenant)

1. `POST /v1/auth/signup` with `{ email, password, full_name, company_name }`
   → store `access_token`, `refresh_token`.
2. `GET /v1/me` → confirm `role=public` and the `company_id` is set.
3. (Admin task) `PATCH /v1/companies/me/credentials/<provider>` with the
   provider credentials → renders `CredentialMetadataOut`.
4. Moabits only — link each local company to a Moabits company code:
   - Fast table of all companies + their current mapping status:
     `GET /v1/companies/provider-mappings/moabits`
   - Fast table of cached Moabits source companies:
     `GET /v1/companies/provider-mappings/moabits/source-companies`
   - To create or edit a link, open the discovery view (live Moabits call):
     `GET /v1/companies/provider-mappings/moabits/discover`
   - Then persist the link for one local company:
     `PUT /v1/companies/{company_id}/provider-mappings/moabits`
   - To rotate only the Moabits `x_api_key` (managers):
     `PATCH /v1/companies/me/credentials/moabits` with `{"credentials": {"x_api_key": "..."}}`
5. `GET /v1/sims?provider=<provider>` (Tele2 needs `modified_since`).

### Find a SIM by ICCID (when provider is unknown)

Use the global ICCID search — do **not** try each `?provider=` variant:

```
GET /v1/sims?iccid=8988216716970004975
```

- If the SIM is in the routing map → one provider call, `partial: false`.
- If not → providers that support ICCID filtering are queried in parallel.
  Provider failures are reported in `failed_providers` and
  `provider_statuses`.
- `items` will contain the SIM if it was found at any searched provider.
- `items` empty + `partial: false` → SIM not found (safe to show "not found").
- `items` empty + `partial: true` → SIM may exist in a provider that failed.

### Display a SIM lifecycle action button

1. `GET /v1/providers/{provider}/capabilities`
2. If `capabilities.set_administrative_status.status === "supported"`,
   build the dropdown from `targets`.
3. On submit, generate a UUID for `Idempotency-Key` and call
   `PUT /v1/sims/{iccid}/status` with `{ "target": "<provider-native-target>" }`.

### Refresh access token before expiry

`expires_in` (seconds) is returned with each `TokenResponse`. Refresh ~60s
before expiry by calling `POST /v1/auth/refresh` with the current
`refresh_token`. The refresh rotates: replace both tokens with the new
pair.

### Paginated SIM list with progressive enrichment *(ADR-012)*

When the list page renders 10–200 rows pulled from `sim_routing_map`, fetch the
full `SubscriptionOut` for each visible row using one batch call instead of N
single-SIM calls:

1. `GET /v1/sims?cursor=...&limit=100` → returns ICCIDs (lightweight, cached
   by the routing map).
2. Render 100 skeleton rows immediately with the ICCIDs.
3. Collect the visible ICCIDs into a `POST /v1/sims/details` request:
   ```json
   { "iccids": ["...", "...", ...] }
   ```
4. As the response arrives, fill each row by `iccid`:
   - `status=ok` → render the SIM card from `data`.
   - `status=timeout` → render a "retry" affordance per row (re-fire just that
     ICCID via `GET /v1/sims/{iccid}` or a smaller batch).
   - `status=rate_limited` → wait `error.retry_after` seconds and retry.
   - `status=not_found` → grey out the row; the SIM was decommissioned at the
     provider since the last routing sync.
5. If `unresolved` is non-empty, prompt the user (or admin) to trigger
   `POST /v1/sync/trigger?provider=<p>` to repopulate the routing map.

TanStack Query keys: key the `useQuery` on the sorted ICCID array; the cache
hit on re-render avoids refiring the same batch.

### Trigger and watch a routing sync *(ADR-012, admin only)*

1. `POST /v1/sync/trigger?provider=tele2` → `202 { job_id, status_url }`.
2. Open a `useQuery({ refetchInterval: 5000 })` against `GET /v1/jobs/{job_id}`.
3. Render `progress.done / progress.total` as a progress bar; show indeterminate
   if `progress.total` is `null`.
4. Stop polling when `status ∈ {done, failed}`.
5. On `done`, refetch `GET /v1/sync/status` to refresh the "Last synced" badge.
6. On `failed`, surface `error.detail`; offer "retry" → repeat from step 1.

### Export thousands of SIMs to CSV *(ADR-012, admin only)*

1. `POST /v1/sims/export` with `{ providers, format: "csv", fields: [...] }`
   → `202 { job_id, status_url }`.
2. Same polling pattern as routing sync (`useQuery({ refetchInterval })`).
3. On `status=done`, redirect to (or download from) `result_url`. The signed
   URL expires at `result_expires_at` (default 24h).
4. On `410 export.result_expired`, the artifact was cleaned up — re-run the
   export.

---

## Files referenced

- Wiring & errors: [app/main.py](app/main.py)
- Auth: [app/identity/routers/auth.py](app/identity/routers/auth.py), [app/identity/auth_utils.py](app/identity/auth_utils.py), [app/identity/dependencies.py](app/identity/dependencies.py)
- Profiles & users: [app/identity/routers/me.py](app/identity/routers/me.py), [app/identity/routers/users.py](app/identity/routers/users.py), [app/identity/schemas/](app/identity/schemas/)
- Companies: [app/tenancy/routers/companies.py](app/tenancy/routers/companies.py), [app/tenancy/schemas/company.py](app/tenancy/schemas/company.py)
- Credentials: [app/tenancy/routers/credentials.py](app/tenancy/routers/credentials.py), [app/tenancy/schemas/credentials.py](app/tenancy/schemas/credentials.py), [app/tenancy/credential_expiry.py](app/tenancy/credential_expiry.py)
- SIMs: [app/subscriptions/routers/sims.py](app/subscriptions/routers/sims.py), [app/subscriptions/schemas/sim.py](app/subscriptions/schemas/sim.py), [app/subscriptions/domain.py](app/subscriptions/domain.py)
- Providers: [app/providers/routers.py](app/providers/routers.py), [app/providers/base.py](app/providers/base.py), [app/providers/schemas.py](app/providers/schemas.py)
- Async jobs & sync (ADR-012): [app/sync/](app/sync/), [migrations/008_sync_jobs.sql](migrations/008_sync_jobs.sql)
- Shared: [app/shared/errors.py](app/shared/errors.py), [app/shared/middleware.py](app/shared/middleware.py)
- Config & env: [app/config.py](app/config.py)
